"""
Shared configuration scaffolding.
"""

from dataclasses import dataclass


@dataclass(frozen=True)
class Settings:
    """Project-wide settings placeholder."""

    # Add config fields (env vars, file paths, API keys) here.
    pass
